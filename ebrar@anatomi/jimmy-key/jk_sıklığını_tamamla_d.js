const jk_sıklığını_tamamla_html = () => {
    const html = `
    `;
  }
  const jk_sıklığını_tamamla_css =() => {
    const style = `
      <style>
    </style>
    `;
    const head = document.querySelector("head");
    head.insertAdjacentHTML("beforeend", style);
  }
  const jk_sıklığını_tamamla_js = () => {
  }
  const jk_sıklığını_tamamla_init = () => {
    jk_sıklığını_tamamla_html();
    jk_sıklığını_tamamla_css();
    jk_sıklığını_tamamla_js();
  }
  const jk_sıklığını_tamamla_condition = () => {
  }
  if(jk_sıklığını_tamamla_condition()){
    jk_sıklığını_tamamla_init();
  }